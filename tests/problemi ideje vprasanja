3 problem spremenljivke addition.aeff problem Runtime error: Integer expected but got a. a je topdef in tipa int.
4 vprašanje scope parametrov?
7 problem tip [](ali katere druge variante)
        kaj če imamo
        let a = [] in //tipa 'a4 list
        let b = 5::a in //a je tipa 'a7 list, b je tipa int list kjer je 'a7 = int 
        let c = "5"::a in //a je tipa 'a15 list, c je tipa string list kjer je 'a15 = string 
        ...

11 vprašanje če imamo funkcijo f tipa int -> a. Kašnega tipa je f 5 ? 1) Kaj bi moralo biti? 2) kako izračunamo?

17 vprašanja substitutions
        d) Dovolimo

        let (id : 'a->'a) a=a
        let (singleton : a'->['a]) a = a::[]

19 Matija 
        let (id : 'a -> 'a) x = x
        let (compare : 'a -> 'b -> bool) a b =
                let a1 = id a in
                1a = b //tu je napaka uporabnika (mislil je a1 namesto 1a). 
                        //To napako aeff napačno zazna in se pritoži, da ne more 1 uporabiti na a.
                        // Aeff bi moral reči spremenljivka 1a ne obstaja. 
        run compare 5 "7"

20      let (compare : 'a -> 'b -> bool) a b =
                let (id : 'a -> 'a) x = x in
                let a1 = id a in
                a1 = b
        run compare 5 "7"

        vrne error medtem ko

        let (id : 'a -> 'a) x = x
        let (compare : 'a -> 'b -> bool) a b =
                let a1 = id a in
                a1 = b
        run compare 5 "7"

        ga ne. Zakaj?

21      let (compare : 'c -> 'd -> bool) a b =
                let a1 = a in
                a1 = b
        run compare 5 9
        Izpiše 'c kot \alpha in 'd kot \beta


22      Zakaj desugar ne obdrži loc? kako naj vrnem kašen pameten error brez loc?